#include <stdio.h>
#include <stdlib.h>
#include "sort.h"

void merge(int *lNumber, int lLen, int *rNumber, int rLen); //函数声明
void divide(int *number, int len);
void mergeSort(int)

    int main(int argc, const char **argv)
{
    printf("");
    merge(int a);
    return 0;
}
void merge(int *lNumber, int ln, int *rNumber, int rn); //函数声明
{
    
}

void divide(int *number, int n)
{
    merge(numebr,n/2,number+(n/2),n-(n/2));
    mergeSort()
}
//函数
void mergeSort(int *a, int n)
{
    if (n > 2)
    {
        divide(a, n);

    }
    else if (n = 2)
    {
        if (*a < *(a + 1))
        {
            swap(a, a + 1);
        }
    }
}